"""Deprecated and unused.

The pipeline-diagram rendering/saving feature was removed per request — this
file is no longer imported anywhere. Safe to delete manually (the sandbox
this assistant runs in can't delete files in your mounted folder, only
write/edit them).
"""
