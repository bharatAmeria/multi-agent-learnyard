# Requirement Agent Prompt

Convert the user's raw request into a structured software specification.

Input: `user_requirement` (free text)
Output: `project_type`, `summary`, `key_features` (JSON matching `StructuredRequirement`)

(Fill in the actual system/user prompt text here during Phase 2.)
